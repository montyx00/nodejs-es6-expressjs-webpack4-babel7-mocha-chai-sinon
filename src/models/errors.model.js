let ErrorModel = {
    algorithm: {
        missingAlgorithmException: {
            code: "111",
            message: "The requested algorithm is not exist"
        }
    }
}

export default ErrorModel